import {PackageTree} from './PackageTree';

// File to output to user

function outputToUser(packageTree: PackageTree) {
  // Output contents of PackageTree in a user friendly manner
  throw new Error('not implemented');
}